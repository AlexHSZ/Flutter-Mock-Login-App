import 'package:flutter/material.dart';

class App extends StatelessWidget {
  Widget build(context) {
    return MaterialApp(
      title: "Login Demo App",
      home: Scaffold(
        body: Text("Form under constructions"),
      ),
    );
  }
}
